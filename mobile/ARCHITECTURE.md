# Smart Faucet Mobile App - Visual Overview

## 🏗 Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         App.tsx                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │         QueryClientProvider (React Query)            │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │          PaperProvider (UI Theme)             │   │   │
│  │  │  ┌───────────────────────────────────────┐   │   │   │
│  │  │  │      AuthProvider (Auth State)        │   │   │   │
│  │  │  │  ┌────────────────────────────────┐  │   │   │   │
│  │  │  │  │     RootNavigator              │  │   │   │   │
│  │  │  │  │  ┌──────────────────────────┐  │  │   │   │   │
│  │  │  │  │  │ isAuthenticated?         │  │  │   │   │   │
│  │  │  │  │  │  Yes → AppTabs          │  │  │   │   │   │
│  │  │  │  │  │  No  → AuthStack        │  │  │   │   │   │
│  │  │  │  │  └──────────────────────────┘  │  │   │   │   │
│  │  │  │  └────────────────────────────────┘  │   │   │   │
│  │  │  └───────────────────────────────────────┘   │   │   │
│  │  └──────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Screen Flow

### Authentication Flow
```
┌────────────┐
│  Landing   │
│   Screen   │ (Optional)
└─────┬──────┘
      │
      ▼
┌────────────┐         ┌────────────┐
│   Login    │────────▶│   Signup   │
│   Screen   │◀────────│   Screen   │
└─────┬──────┘         └────────────┘
      │
      │ Success
      ▼
┌────────────────────────────────────┐
│         App Tabs Navigator          │
│  ┌──────┬──────┬────────┬────────┐ │
│  │ Dash │Device│ Alerts │Profile │ │
│  │board │  s   │        │        │ │
│  └──────┴──────┴────────┴────────┘ │
└────────────────────────────────────┘
```

### Main App Navigation
```
┌─────────────────────────────────────────────────────────────┐
│                       Bottom Tabs                            │
├─────────┬──────────┬──────────┬──────────┬─────────────────┤
│Dashboard│ Devices  │  Alerts  │ Profile  │                  │
│   Tab   │   Tab    │   Tab    │   Tab    │                  │
└─────┬───┴────┬─────┴────┬─────┴────┬─────┘                  │
      │        │          │          │                         │
      ▼        │          │          ▼                         │
  Dashboard    │          │     Profile Screen                 │
   Screen      │          │          │                         │
      │        │          │          ├──▶ Admin Panel (Admin)  │
      │        │          │          │                         │
      │        ▼          ▼          └──▶ Logout               │
      │   Devices     Alerts                                   │
      │   Screen      Screen                                   │
      │        │                                                │
      │        └──▶ Device Detail Screen                       │
      │                   │                                     │
      │                   ├──▶ Send Commands                   │
      │                   ├──▶ View Charts                     │
      │                   └──▶ Command History                 │
      │                                                         │
      └──▶ Quick Actions on Device Cards                       │
```

## 🔐 Authentication Flow Detail

```
┌─────────────┐
│ User enters │
│ credentials │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Validate form   │
│ (zod schema)    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ POST /auth/login│
└──────┬──────────┘
       │
       ├─── Success ───────────────────────────┐
       │                                        ▼
       │                           ┌──────────────────────┐
       │                           │ Save tokens to       │
       │                           │ SecureStore:         │
       │                           │ - accessToken        │
       │                           │ - refreshToken       │
       │                           │ - user info          │
       │                           └──────┬───────────────┘
       │                                  │
       │                                  ▼
       │                           ┌──────────────────────┐
       │                           │ Set AuthContext user │
       │                           └──────┬───────────────┘
       │                                  │
       │                                  ▼
       │                           ┌──────────────────────┐
       │                           │ Navigate to AppTabs  │
       │                           └──────────────────────┘
       │
       └─── Error ─────────────────────────┐
                                            ▼
                                 ┌──────────────────────┐
                                 │ Show error message   │
                                 │ Allow retry          │
                                 └──────────────────────┘
```

## 🔄 API Request Flow

```
┌────────────┐
│ Component  │
│ calls API  │
└─────┬──────┘
      │
      ▼
┌─────────────────┐
│ React Query     │
│ (useQuery/      │
│  useMutation)   │
└─────┬───────────┘
      │
      ▼
┌─────────────────────────────────────────┐
│ API Client (Axios)                      │
│ ┌─────────────────────────────────────┐ │
│ │ Request Interceptor:                │ │
│ │ - Get token from SecureStore        │ │
│ │ - Add Authorization header          │ │
│ └───────────┬─────────────────────────┘ │
│             ▼                            │
│ ┌─────────────────────────────────────┐ │
│ │ Make HTTP Request                    │ │
│ └───────────┬─────────────────────────┘ │
│             ▼                            │
│ ┌─────────────────────────────────────┐ │
│ │ Response Interceptor:               │ │
│ │ - Check for 401                     │ │
│ │ - If 401: try refresh token         │ │
│ │ - If refresh fails: logout          │ │
│ │ - If 401 but refreshing: queue req  │ │
│ └───────────┬─────────────────────────┘ │
└─────────────┼─────────────────────────────┘
              │
              ▼
        ┌───────────┐
        │  Success  │────▶ Update UI
        └───────────┘
              │
        ┌───────────┐
        │   Error   │────▶ Show error + retry
        └───────────┘
```

## 📊 Device Detail Screen Layout

```
┌──────────────────────────────────────────┐
│ ◀ Back      Device Details               │
├──────────────────────────────────────────┤
│ ┌────────────────────────────────────┐   │
│ │ Device Name            [Active]    │   │
│ │ Location: Kitchen                  │   │
│ │ UID: FAU001                        │   │
│ │ Last seen: 2 mins ago              │   │
│ │ Firmware: 1.2.3                    │   │
│ └────────────────────────────────────┘   │
│                                           │
│ ┌────────── Controls ──────────┐         │
│ │ [Open] [Close] [Shut Off]    │         │
│ │ [Set Flow Rate]              │         │
│ └──────────────────────────────┘         │
│                                           │
│ ┌────────── Analytics ─────────┐         │
│ │ [24h] [7d] [30d]             │         │
│ │                              │         │
│ │  Flow Rate Chart             │         │
│ │  ▁▂▃▅▆▇▆▅▃▂▁                │         │
│ │                              │         │
│ │  Volume Chart                │         │
│ │  ▁▁▂▂▃▃▄▄▅▅▆                │         │
│ └──────────────────────────────┘         │
│                                           │
│ ┌────── Recent Commands ───────┐         │
│ │ OPEN_VALVE    [ACK]  10:30   │         │
│ │ SET_FLOW_RATE [SENT] 10:25   │         │
│ │ CLOSE_VALVE   [ACK]  10:20   │         │
│ └──────────────────────────────┘         │
└──────────────────────────────────────────┘
```

## 🎨 Component Hierarchy

```
App
├── QueryClientProvider
│   ├── PaperProvider
│   │   └── AuthProvider
│   │       └── RootNavigator
│   │           ├── AuthStack (if not authenticated)
│   │           │   ├── LoginScreen
│   │           │   │   ├── Form (react-hook-form)
│   │           │   │   └── Snackbar (errors)
│   │           │   └── SignupScreen
│   │           │       ├── Form (react-hook-form)
│   │           │       └── Snackbar (errors)
│   │           │
│   │           └── AppTabs (if authenticated)
│   │               ├── DashboardTab
│   │               │   └── DashboardScreen
│   │               │       ├── KPIWidget (x4)
│   │               │       ├── DeviceCard (x3)
│   │               │       └── ConfirmModal
│   │               │
│   │               ├── DevicesTab (Stack)
│   │               │   ├── DevicesScreen
│   │               │   │   ├── Searchbar
│   │               │   │   ├── FlatList
│   │               │   │   └── DeviceCard (each)
│   │               │   │
│   │               │   └── DeviceDetailScreen
│   │               │       ├── StatusBadge
│   │               │       ├── Control Buttons
│   │               │       ├── FlowRateSlider
│   │               │       ├── Charts (LineChart x2)
│   │               │       ├── Command List
│   │               │       └── ConfirmModal
│   │               │
│   │               ├── AlertsTab
│   │               │   └── AlertsScreen
│   │               │       ├── SegmentedButtons
│   │               │       ├── FlatList
│   │               │       └── AlertCard (each)
│   │               │
│   │               └── ProfileTab (Stack)
│   │                   ├── ProfileScreen
│   │                   │   ├── Avatar
│   │                   │   ├── Info Cards
│   │                   │   ├── Action Buttons
│   │                   │   └── Change Password Dialog
│   │                   │
│   │                   └── AdminPanelScreen (if admin)
│   │                       └── Management Cards
```

## 💾 Data Flow with React Query

```
Component
    │
    ├──▶ useQuery(['devices'])
    │       │
    │       ├──▶ devicesApi.getDevices()
    │       │       │
    │       │       └──▶ axios.get('/devices')
    │       │               │
    │       │               └──▶ Backend API
    │       │
    │       └──▶ React Query Cache
    │               │
    │               ├─ staleTime: 30s
    │               ├─ cacheTime: 5m
    │               └─ automatic refetch
    │
    └──▶ useMutation(sendCommand)
            │
            ├──▶ commandsApi.sendCommand()
            │       │
            │       └──▶ axios.post('/commands')
            │
            └──▶ onSuccess:
                    └──▶ invalidateQueries(['commands'])
                            │
                            └──▶ Automatic refetch
```

## 🔒 Security Layers

```
┌────────────────────────────────────────┐
│         User Authentication             │
│  (email + password with validation)     │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│       JWT Token Generation              │
│         (by Backend)                    │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│      Secure Token Storage               │
│  (Expo SecureStore)                     │
│  - iOS: Keychain                        │
│  - Android: Keystore                    │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│    Token Attached to Requests           │
│  (Authorization: Bearer <token>)        │
└──────────────┬─────────────────────────┘
               │
               ▼
┌────────────────────────────────────────┐
│      Backend Authorization              │
│  (JWT validation + role check)          │
└────────────────────────────────────────┘
```

## 📱 Screen Sizes & Responsive Design

```
┌─────────────────────────────────────┐
│ Small Phone (< 375px)               │
│ - Single column layout              │
│ - Compact spacing                   │
│ - Smaller fonts                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Regular Phone (375-414px)           │
│ - Standard layout                   │
│ - Default spacing                   │
│ - Standard fonts                    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ Large Phone / Tablet (> 414px)      │
│ - Multi-column where appropriate    │
│ - Generous spacing                  │
│ - Larger fonts                      │
└─────────────────────────────────────┘
```

## 🎯 Feature Coverage

```
Web App Features          Mobile App
─────────────────────────────────────
✅ Login/Signup          → ✅ Implemented
✅ Dashboard             → ✅ Implemented
✅ Device List           → ✅ Implemented
✅ Device Details        → ✅ Implemented
✅ Send Commands         → ✅ Implemented
✅ View Readings         → ✅ Implemented
✅ Charts                → ✅ Implemented
✅ Alerts                → ✅ Implemented
✅ Profile               → ✅ Implemented
✅ Change Password       → ✅ Implemented
✅ Admin Features        → ✅ Basic (Stub)
✅ Logout                → ✅ Implemented
```

This visual overview shows the complete architecture and data flow of the mobile application!
